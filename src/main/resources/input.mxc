function void main() {

  matrix m = {
    {2|1, 1|1, -1|1},
    {-3|1, -1|1, 2|1},
    {-2|1, 1|1, 2|1}
  };

  matrix v = {
    {8|1, -11|1, -3|1}
  };

  matrix result = gauss(m, v);

  print_matrix(result);

  matrix result2 = solve(m, v);

  print_matrix(result2);
}
