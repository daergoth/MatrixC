rational a = 1|1;

function rational asd(rational as) {

  rational b = 0|1;

  if (b) {
    if (as) {
      rational h = 1|1;
      return h;
    } else {
      rational g = 2|1;
      return g;
    }
  } else {

    return b;
  }

}

function matrix mat() {
  matrix m = {{1|1, 4|1}, {2|1, 2|1}};

  return m;
}

function void main() {
  a = 1|2;

  rational addition = a / a;

  print_rational(addition);

  print_matrix(mat());
  print_matrix(mat() a);
}
